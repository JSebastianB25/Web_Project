# zglobals/apps.py
from django.apps import AppConfig

class ZglobalsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'uglobals' # ¡IMPORTANTE! Debe coincidir con el nombre de tu app